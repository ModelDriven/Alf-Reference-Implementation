/**
 * Test return statements.
 **/
activity Statements_Return(): Integer {

  // Setup
  item = 4;
  index = 1;
  list = Integer[]{1,2,3};
  x = 1;
  // *****
  
  return item;
  return list[index];
  return x * x;
}
