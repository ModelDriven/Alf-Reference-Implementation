private import Super;
class SuperConstructors specializes Super {
  @Create public SuperConstructors(in x: Integer) {
    super.create1(x);
  }

  @Create public B0() {
    this.B1(0);
  }
  @Create public B1(in x: Integer) {
    super(x);
  }

  @Create public B2() {
    this(0);
  }
}
