class Destructors specializes FoundationalModelLibrary::BasicInputOutput::Channel {
  @Destroy public destroy();
  
  @Destroy private unregister() {
    Repository::get().unregister(this);
    super.destroy();
  }
  
  @Destroy public D() {
    super.destroy();
  }

  @Destroy public D (in reason: String) {
    WriteLine(reason);
    this.destroy();
  }
}
