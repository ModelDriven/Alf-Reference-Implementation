namespace Identifiers::'Identifier Factory';

/** Generate a unique identifier from the next sequential integer. */
activity 'get next identifier'(): String {
  return (this.'next identifier'++).toString();            // See Note 1
}
